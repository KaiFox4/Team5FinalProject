#include <iostream>
#include <cmath>
using namespace std;

// there are 5 lights total 

if(power_level < 20 && power_level >= 0)
  {
 // light 1 is on
  }
else if(power_level < 40 && power_level >= 20)
  {
 // lights 1 and 2 are on
  }
else if(power_level < 60 && power_level >= 40)
  {
 // lights 1-3 are on
  }
else if(power_level < 80 && power_level >= 60)
  {
  // lights 1-4 are on
  }
else if(power_level <= 100 && power_level >= 80)
  {
  // all the lights are on
  }

