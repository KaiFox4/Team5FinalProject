#include <iostream>
#include <cmath>
using namespace std;

// there are 5 lights total 

if(power_level <= 100 && power_level >= 80)
{
  // all the lights are on
}
else if(power_level < 80 && power_level >= 60)
{
  // lights 1-4 are on
}
